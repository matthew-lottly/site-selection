"""Houston Family Dollar site-selection pipeline.

A class-based rewrite of the original numbered-script pipeline: every stage
is a PipelineStage subclass under pipeline.stages, run in order by
pipeline.core.Pipeline (see run_pipeline.py at the repo root).
"""
